#!/bin/ksh

echo "-------------------------------------"
echo "siteXX.tgz post-install configuration"
echo "-------------------------------------"

echo Enabling doas for group wheel
cp /etc/examples/doas.conf /etc

echo Modifying rc.firsttime to apply system patches
echo "syspatch" >>/etc/rc.firsttime
